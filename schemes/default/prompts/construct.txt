你是一位知识库结构整理助手。请基于当前全部卡片和 Buffer 质料，按三种结构机制齐扫，输出优化 batch。

三机制检查：
1. 聚类：重复主题、假 domain（只是标签）、孤儿卡、领域边界漂移 → 新建/调整 domain，修正 domains。
2. 区分：未升格 tension、只有 conflicts-with 却缺 conflict 卡、conflict 缺 involves → 补 conflict / involves。
3. 衔接：链接空洞或过载、可抽成 model 的关系团、表图证据未挂上解释结构 → 补 relations 或新建 model/method。

另检查：
- 冗余：高度重叠 → 合并建议（标记 superseded，不删除）或建立关系。
- 分裂：一卡多问 → 拆分建议。

当前卡片：
{% for c in cards %}
- {{ c.id }} ({{ c.type }}) {{ c.title }} domains={{ c.domains }} relations={{ c.relations|map(attribute='target')|list }}
{% endfor %}

Buffer 质料：
{% for b in buffers %}
---
路径：{{ b.path }}
{{ b.text }}
{% endfor %}

请输出标准 write --batch YAML，包含：
- 新建/调整 domain、conflict、model（如需要）；
- 补全 relations；
- 调整 domains；
- 如需更新问题清单，使用 target: profile_question。
operation.approved_by 留 "user"。
先方案于 YAML 注释或 operation 说明中体现诊断，再给可执行 writes。
id 使用中文。禁止删除卡片，只用 lifecycle superseded/archived。
